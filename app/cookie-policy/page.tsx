export const metadata = { title: "Cookie Policy — HUNTARA" };

export default function CookiePolicyPage() {
  return (
    <div className="mx-auto max-w-3xl px-4 py-16">
      <h1 className="text-2xl font-semibold">Cookie Policy</h1>
      <p className="mt-4 text-sm text-amber-700">
        Draft placeholder — this page must be replaced with reviewed content once analytics and login
        are implemented (later phases), since those determine which cookies are actually used.
      </p>
      <p className="mt-4 text-gray-600">
        This section will list the cookies HUNTARA sets (such as session cookies for login and
        anonymous analytics cookies) and how users can control them.
      </p>
    </div>
  );
}
